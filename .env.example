# Server only, used by the /api/llm function (Vercel serverless).
# Set this in the Vercel project's environment variables. Never exposed to the client.
# With it set, "Live conversations" in Settings makes the opponent respond to
# the player's actual words. Without it, the game falls back to offline scoring.
ANTHROPIC_API_KEY=
